{"ts":"2026-04-05T00:00:00Z","kind":"phase_start","phase":1,"goal":"ClaudeAdapter non-streaming, single conversation","source":"ROADMAP.md#phase-1"}
{"ts":"2026-04-05T00:00:01Z","kind":"decision","subject":"process_model","what":"no persistent CLI processes; each turn is a fresh shell-out","why":"2026-04-05 spike showed all three CLIs expose non-interactive JSONL mode with session resumption; pty/tmux was dead code","source":"PLAN.md capability matrix"}
{"ts":"2026-04-05T00:00:02Z","kind":"decision","subject":"concurrency","what":"one in-flight turn per conversation, FIFO queue","why":"CLIs are single-threaded interactive processes; principle #1 seam requires serialization above the adapter","source":"PLAN.md design decision #1"}
{"ts":"2026-04-05T00:00:03Z","kind":"decision","subject":"filesystem","what":"each CLI turn runs in an ephemeral scratch cwd; no access to user files","why":"OpenAI chat API has no cwd concept; only coherent interpretation is to suppress CLI native file tools at the sandbox","source":"PLAN.md design decision #3"}
{"ts":"2026-04-05T00:00:04Z","kind":"lesson","severity":"high","subject":"claude_exit_code","text":"claude -p exits 0 even when a rate_limit_event with status != allowed was emitted. Never infer health from exit code; always inspect the JSONL event stream for rate_limit_event and result.subtype.","source":"PLAN.md quota section"}
{"ts":"2026-04-05T00:00:05Z","kind":"lesson","severity":"high","subject":"cold_cache_tax","text":"Cold CLI invocation eats 6k (gemini) to 14k (codex) input tokens of agent-prompt overhead before the user's prompt is even processed. claude's prompt-cache window is 1h. If jRouter idles >1h, first turn back pays the full tax. Warm-cache discipline is a routing concern, not just an optimization.","source":"PLAN.md capability matrix"}
{"ts":"2026-04-05T00:00:06Z","kind":"lesson","severity":"medium","subject":"agent_contamination","text":"Under OAuth subscription auth, the backend CLIs run as tool-wielding agents framed by their own system prompts. A trivial prompt ('reply ok') produced num_turns:3 in the spike — internal agent work the client didn't request. This is observable (log num_turns and tool events per turn) but not preventable. Design for it, don't fight it.","source":"PLAN.md principle revision"}
{"ts":"2026-04-05T00:00:07Z","kind":"lesson","severity":"medium","subject":"gemini_compound","text":"Gemini auto-routes across sub-models within a single turn (spike saw gemini-3-flash-preview and gemini-2.5-flash-lite in the same call). stats.models has the per-model breakdown. GeminiAdapter must surface this as a compound-provider quota event, not a single counter.","source":"PLAN.md capability matrix"}
{"ts":"2026-04-05T00:00:08Z","kind":"lesson","severity":"medium","subject":"session_id_shapes","text":"Three vendors, three session id shapes: claude accepts a client-chosen UUID via --session-id; gemini returns a server-assigned index in its init event; codex returns a server-assigned thread_id in thread.started. The adapter boundary must normalize this — the router stores an opaque string and never parses it.","source":"PLAN.md capability matrix"}
{"ts":"2026-04-05T00:00:09Z","kind":"step_start","phase":1,"step":"1.0","goal":"scaffold the orientation loop (STATUS, JOURNAL, CLAUDE.md, scripts/)"}
{"ts": "2026-04-18T08:06:54Z", "kind": "decision", "text": "canonical project name is FreelOAder (package: freeloader); old 'jRouter'/'jrouter' references in non-journal files have been renamed", "subject": "project_name", "why": "repo dir, PLAN.md, README.md all use freeloader; the jrouter references in STATUS/ROADMAP/AGENT/CLAUDE/scripts were drift from a prior name and would break every gate on the first commit that creates src/", "source": "STATUS.md, ROADMAP.md, scripts/gate_*.sh"}
{"ts": "2026-04-18T08:07:02Z", "kind": "decision", "text": "CLIAdapter.send() Protocol carries system: str | None = None from day one", "subject": "system_prompt_slot", "why": "phase-5 tool-call shim requires adapter-owned system-prompt slot (PLAN.md § 'Things to explicitly *not* do'); retrofitting the signature after ClaudeAdapter/GeminiAdapter/CodexAdapter exist is a three-way rewrite plus three contract-test rebaselines", "source": "PLAN.md principle #1"}
{"ts": "2026-04-18T08:07:08Z", "kind": "decision", "text": "chat-completions history diff lives in src/freeloader/canonical/history_diff.py — not the frontend, not the adapter", "subject": "history_diff_home", "why": "principle #6 says handlers are ~50 lines; decision #3 says adapters are text-in/text-out. Client edits/regenerations/reordered system messages need a real home. Canonical layer is the only coherent owner", "source": "PLAN.md principle #4, decision recorded in phase-1 exit criteria"}
{"ts": "2026-04-18T08:20:34Z", "kind": "lesson", "text": "Anthropic ToS crackdown (Jan 9, 2026 server-side block; Feb 19 policy; Apr 4 OpenClaw ban) prohibits OAuth tokens from Free/Pro/Max accounts being used in 'any other product, tool, or service' including the Agent SDK. Enforcement was server-side client-identity validation rejecting spoofed Claude Code headers — the OpenClaw-style pattern of extracting the OAuth token and calling api.anthropic.com directly. BUT Anthropic staff clarified that running the genuine claude binary as a subprocess is still allowed ('even on a remote server, even over SSH'); CLAUDE_CODE_OAUTH_TOKEN is officially documented for CI/scripts. FreelOAder's MVP path (shell-out to claude -p --output-format stream-json) is the sanctioned pattern, not the banned one. --bare OAuth path is dead (confirmed). Remaining risks: (a) the written ToS is stricter than the staff clarification; (b) commercial/shared use requires API keys; (c) cold-cache tax >1h is the behavioral signature Anthropic flags wrappers by — warm-cache discipline per session_id is a ToS concern, not just cost.", "severity": "high", "subject": "anthropic_3p_ban", "source": "https://x.com/bcherny/status/2040206440556826908, https://code.claude.com/docs/en/authentication, https://thenewstack.io/anthropic-agent-sdk-confusion/"}
{"ts": "2026-04-23T13:30:34Z", "kind": "step_done", "text": "phase 1 step 1.0 scaffolding complete — all loop artifacts + scripts committed in 8e01c36..17dd2dd; stale 'first commit' checkbox in prior STATUS was an artifact of the cold-start procedure", "phase": "1", "step": "1.0"}
{"ts": "2026-04-23T13:30:43Z", "kind": "decision", "text": "insert step 1.0.5 (architecture review) between 1.0 and 1.1 — adversarial review of PLAN.md + ROADMAP.md before first code lands; chosen as a step rather than a full phase to avoid re-litigating settled ground and to match the loop shape (AGENT.md § scope discipline)", "subject": "architecture_review_step", "why": "PLAN.md encodes 8 principles + 11 decisions + spike findings; after step 1.1 those harden into code. Cost of revising a PLAN.md line now is an edit; cost after src/freeloader/adapters/claude.py exists is a refactor plus test rebaseline. One-shot review step with JOURNAL entries as output — no new gate script, no new phase.", "source": "STATUS.md#step-1.0.5"}
{"ts": "2026-04-23T13:30:45Z", "kind": "step_start", "text": "architecture review — adversarial pass over PLAN.md + ROADMAP.md before first code lands", "phase": "1", "step": "1.0.5"}
{"ts": "2026-04-23T13:45:32Z", "kind": "decision", "text": "Delta is a tagged union (TextDelta | SessionIdDelta | UsageDelta | RateLimitDelta); frontend pattern-matches per variant", "subject": "delta_tagged_union", "why": "flat Delta with 'last one has session id' semantics conflates content stream and control channel. Every adapter interleaves text, session id, usage, and rate-limit events on a single JSONL stream; variants must be distinguishable at every yield. PLAN.md principle #1 revised.", "source": "PLAN.md § principle 1"}
{"ts": "2026-04-23T13:45:33Z", "kind": "decision", "text": "drop 'logged' as a separate terminal state; journal write is atomic with reaching a terminal state (complete / cancelled / backend_error / rate_limited / timed_out)", "subject": "turn_state_simplified", "why": "a 'drained but not yet logged' window creates a consistency gap — if the process crashes there, conversation history and JOURNAL disagree on the outcome. Journal-write failures surface as adapter_error. PLAN.md principle #2 revised.", "source": "PLAN.md § principle 2"}
{"ts": "2026-04-23T13:45:33Z", "kind": "decision", "text": "history_diff MVP scope frozen at three cases (append, last-assistant regeneration, mismatch→raise); mid-history edits raise 400", "subject": "history_diff_mvp_scope", "why": "the fourth case (truncate stored history at divergence, replay from N onward) is cheap to add once the three-case spine works. Pre-building it is scope creep per AGENT.md § scope discipline. Revisit in phase 5 if a real client surfaces the need.", "source": "PLAN.md § principle 4"}
{"ts": "2026-04-23T13:47:30Z", "kind": "decision", "text": "two logs, one format: global JOURNAL.jsonl for all events (build-time + per-turn + quota signals); per-conversation <data_dir>/<conversation_id>.jsonl for canonical messages only (replay on rebind). Router reads JOURNAL.", "subject": "storage_layout", "why": "principle #5 says quota is an event stream the router consumes; scanning N per-conversation files per routing decision doesn't scale. Separating events (global) from messages (per-conversation) matches the two access patterns. PLAN.md decision #6 revised.", "source": "PLAN.md § decision 6"}
{"ts": "2026-04-23T13:47:30Z", "kind": "decision", "text": "canonical replay into a new backend contains only user messages and final-assistant-message turns; intermediate agent contamination preserved in metadata but not replayed", "subject": "replay_scope", "why": "OpenAI chat surface only exposes user + assistant roles. claude's num_turns>1 from a single client turn (internal tool work) was never part of the conversation the client thinks it's having. Lossy for the backend reasoning trace; faithful to the client-visible conversation. PLAN.md principle #3 revised.", "source": "PLAN.md § principle 3"}
{"ts": "2026-04-23T13:47:30Z", "kind": "decision", "text": "no health() on CLIAdapter Protocol in MVP; phase 4 may add probe_quota() only if routing needs a decision before the first turn fires", "subject": "health_probe_deferred", "why": "principle #5 says quota is an event stream, not a probe. health() with ambiguous QuotaState return was conflating liveness (the subprocess) with quota (the subscription). Subprocess liveness is measured per-turn (did it spawn, did it exit with result). Remove now; retrofit cost after phase-1 code lands is one-time. PLAN.md principle #1 revised.", "source": "PLAN.md § principle 1"}
{"ts": "2026-04-23T13:47:47Z", "kind": "decision", "text": "HTTP framework: FastAPI (async-native, first-class SSE via StreamingResponse, built-in OpenAPI docs, Pydantic request validation matches OpenAI JSON Schema error shapes)", "subject": "http_framework", "why": "previously only shown in README architecture diagram, never formalized as a decision — gap would let step 1.1 accidentally pick Starlette/Hypercorn. Every OpenAI-compatible proxy example uses FastAPI; client error shapes stay familiar. New PLAN.md decision #12.", "source": "PLAN.md § decision 12"}
{"ts": "2026-04-23T13:47:47Z", "kind": "decision", "text": "runtime logging = stdlib logging; architectural events = JOURNAL.jsonl", "subject": "runtime_logging", "why": "two audiences, two destinations. stdlib logging (default WARNING, INFO with FREELOADER_LOG_LEVEL=info) for server-runtime diagnostics (port binding, spawn failures, stacktraces, request IDs). JOURNAL.jsonl for per-turn records + quota + decisions + phase transitions. Different retention, volume, consumers. No structlog/loguru — stdlib can emit JSON via formatter. New PLAN.md decision #13.", "source": "PLAN.md § decision 13"}
{"ts": "2026-04-23T13:47:47Z", "kind": "decision", "text": "add three scope excluders: no static type checker in gate, no CI/pre-commit, no pytest plugins beyond pytest itself (pytest-asyncio likely exception, decide at step 1.1)", "subject": "scope_excluders_added", "why": "personal-use prototype ~500 lines; mypy/pyright fights Protocol + asyncio + duck-typed adapters for little gain until phase 3. GitHub Actions is overhead the MVP never pays back. Pytest plugins are the usual scope-creep vector. PLAN.md § 'Things to explicitly not do' updated.", "source": "PLAN.md § things to not do"}
{"ts": "2026-04-23T13:48:00Z", "kind": "lesson", "text": "concurrent JOURNAL.jsonl appends within the server process need a single-writer asyncio lock — POSIX O_APPEND is atomic only under PIPE_BUF (~4KB) and a long assistant message in a turn_done record can exceed that. scripts/reflect.sh is single-process so unaffected. Becomes relevant in phase 3+ when multiple turns run concurrently across conversations.", "severity": "medium", "subject": "journal_append_atomicity", "source": "PLAN.md § decision 6, step 1.0.5 review"}
{"ts": "2026-04-23T13:48:00Z", "kind": "lesson", "text": "claude's rate_limit_event has no counterpart 'allowed again' event — once status != 'allowed' the router switches and never switches back without explicit re-enable. resetsAt (unix timestamp) is the signal; phase 4 routing needs a scheduled re-enable (e.g., an asyncio.call_later firing at resetsAt or a polled probe). Without this, the first rate limit makes a backend permanently unreachable for the process lifetime.", "severity": "high", "subject": "rate_limit_stickiness", "source": "PLAN.md § principle 5, step 1.0.5 review"}
{"ts": "2026-04-23T13:48:43Z", "kind": "step_done", "text": "architecture review complete — 13 findings, 7 PLAN.md revisions, 9 decisions + 2 lessons in journal, 1 deferred (/codex consult to step 1.1/1.2)", "phase": "1", "step": "1.0.5"}
{"ts": "2026-04-23T13:56:54Z", "kind": "decision", "text": "phase-3 adapter implementation order: codex first, gemini second (inside phase 3; phase-1 claude unchanged)", "subject": "phase3_adapter_order", "why": "codex is structurally closer to claude — clean JSONL event stream (thread.started / turn.started / item.completed / turn.completed), single-session-per-invocation, server-assigned thread_id string. Gemini is the outlier: compound provider with per-sub-model stats.models breakdown, server-assigned integer index as session id, ambiguous system-prompt injection. Doing codex second exposes what pluralizing the Protocol requires on a low-variance shape before gemini's compound-provider quirk lands. If the Protocol needs revision when the second adapter lands, doing that revision against codex (similar shape to claude) is cheaper than against gemini.", "source": "ROADMAP.md § phase 3, PLAN.md § MVP step 3, README.md § stack"}
{"ts": "2026-04-23T14:18:31Z", "kind": "decision", "text": "delete 'persistent authorized subprocess with stdin/stdout bridged to FreelOAder' text from PLAN.md component description; replace with fresh-subprocess-per-turn wording consistent with spike findings", "subject": "persistent_subprocess_text_removed", "why": "stale text from pre-spike draft contradicted the rest of PLAN.md (capability matrix, principle #2, MVP step 1). First implementer would have built process-handle abstractions instead of per-turn shell-out. Codex P1 finding from /codex consult 1.0.5 pass.", "source": "PLAN.md § component responsibilities, codex P1 #2"}
{"ts": "2026-04-23T14:18:32Z", "kind": "decision", "text": "chat completions conversation identity: SHA-256 hash of message prefix (system messages + first user message), with X-FreelOAder-Conversation-Id header override. Prefix-hash divergence → new conversation, no fuzzy match / LRU recovery", "subject": "conversation_identity", "why": "OpenAI /v1/chat/completions has no conversation_id concept. Without a defined identity strategy, 'diff against stored conversation' (principle #4, phase 1 exit) has no defined lookup key. Hash-of-prefix handles well-behaved clients that resend history verbatim (90% case); optional header handles power users wanting stable ids (10% case). Codex P1 finding from /codex consult 1.0.5 pass.", "source": "PLAN.md § decision 14, codex P1 #1"}
{"ts": "2026-04-23T14:18:32Z", "kind": "decision", "text": "Responses API: FreelOAder-owned opaque response_id (fr_resp_<uuid>); internally maps to (conversation_id, turn_id, backend_session_id at that turn). Backend identity never leaks into OpenAI-facing fields", "subject": "response_id_ownership", "why": "original 'previous_response_id maps directly to backend session id' leaked backend identity into the OpenAI API surface, would break on provider switch / cancellation / retry, and couples phase-3 routing to phase-1 response semantics. FreelOAder-owned id namespace with internal mapping survives provider switches. Codex P1 finding from /codex consult 1.0.5 pass.", "source": "PLAN.md § decision 15, codex P1 #3"}
{"ts": "2026-04-23T14:19:00Z", "kind": "decision", "text": "filesystem sandbox is defense-in-depth via CLI --add-dir/-C flags, NOT a hard boundary. OS-level isolation (sandbox-exec / container) deferred to phase 3+ if threat model demands it", "subject": "filesystem_sandbox_scope", "why": "ROADMAP phase 1 exit originally claimed 'backend cannot read project files' — stronger than the CLI flags actually enforce. --add-dir grants directory access but doesn't deny paths like ~/.ssh, ~/.config, repo parent dirs. Weakening the claim matches reality; strengthening via OS sandbox is real work deferred until it's justified. Codex P1 finding from /codex consult 1.0.5 pass.", "source": "ROADMAP.md § phase 1 exit, codex P1 #4"}
{"ts": "2026-04-23T14:19:00Z", "kind": "decision", "text": "expand Delta union: add FinishDelta, ErrorDelta, RawDelta. Full union = TextDelta | FinishDelta | SessionIdDelta | UsageDelta | RateLimitDelta | ErrorDelta | RawDelta. FinishDelta carries finish_reason (stop/length/content_filter/tool_calls); ErrorDelta terminates stream; RawDelta is the vendor-event escape hatch", "subject": "delta_union_expanded", "why": "original 4-variant union (Text|SessionId|Usage|RateLimit) had no representation for OpenAI finish_reason, adapter-observed errors, or vendor events we haven't canonicalized. Frontend would have accumulated special cases. Adding FinishDelta/ErrorDelta/RawDelta now (7 variants) is cheaper than retrofitting after three adapters exist. Codex P1 finding from /codex consult 1.0.5 pass.", "source": "PLAN.md § principle 1, codex P1 #5"}
{"ts": "2026-04-23T14:19:00Z", "kind": "decision", "text": "split storage into three append-only JSONL logs: repo-root JOURNAL.jsonl (build-time only, tracked in git); <data_dir>/events.jsonl (runtime events, untracked); <data_dir>/conversations/<id>.jsonl (canonical messages, untracked). Router reads events.jsonl, not JOURNAL", "subject": "three_log_split", "why": "previous 'two logs' revision (decision #6 after initial 1.0.5 pass) put runtime events in repo-root JOURNAL.jsonl — running the server would dirty the repo forever, leak conversation metadata into git, and break the append-only-vs-HEAD~1 gate check. Three-log split isolates build-time (git-tracked) from runtime (data_dir). Revises decision #6 for the second time in step 1.0.5. Codex P2 finding from /codex consult 1.0.5 pass.", "source": "PLAN.md § decision 6, codex P2 #3"}
{"ts": "2026-04-23T14:19:01Z", "kind": "decision", "text": "retry only before session_id or text output is observed; post-observation retries must start a fresh backend session (no -r) and flag prior attempt 'ambiguous' in runtime event log. Router treats 'ambiguous' as stronger evidence of unhealth than clean failure", "subject": "retry_idempotency", "why": "original decision #8 allowed 'retry once against the same backend' without distinguishing pre- vs post-observation. If CLI assigned session id or emitted partial text, retrying with -r forks or duplicates turn state. Duplicated user turns silently corrupt conversation history. Codex P2 finding from /codex consult 1.0.5 pass.", "source": "PLAN.md § decision 8, codex P2 #4"}
{"ts": "2026-04-23T14:19:01Z", "kind": "decision", "text": "gate_common.sh invokes ruff/pytest via 'uv run' rather than PATH lookup. Bootstrap python3 check for JOURNAL validity stays as system python3 (runs before pyproject.toml exists)", "subject": "gate_uv_wrapper", "why": "user's ~/.claude/CLAUDE.md enforces uv-first for all project Python tooling. Direct 'ruff' / 'pytest' invocation in gate_common relied on global ruff/pytest on PATH, which isn't guaranteed in a clean checkout. 'uv run' resolves to the project's locked toolchain. Codex P2 finding from /codex consult 1.0.5 pass.", "source": "scripts/gate_common.sh, codex P2 #1"}
{"ts": "2026-04-23T14:19:28Z", "kind": "lesson", "text": "OpenAI request fields beyond messages/stream/model are not yet decided: temperature, max_tokens, stop, response_format, tool_choice, multimodal blocks, stream_options, parallel_tool_calls, metadata. Silent ignore creates hard-to-debug client behavior (client sets temperature, FreelOAder drops it, output looks wrong but no error). Phase 2 decision: enumerate which fields are honored / ignored-with-warning / rejected. Codex P3 finding from /codex consult 1.0.5 pass.", "severity": "medium", "subject": "openai_field_passthrough", "source": "codex P3 #1, defer to phase 2"}
{"ts": "2026-04-23T14:20:12Z", "kind": "step_done", "text": "architecture review round 2 complete (/codex consult) — 10 findings, 6 PLAN revisions, 3 ROADMAP revisions, 1 script revision, 8 decisions, 1 lesson. Step 1.0.5 totals: 23 findings, 17 decisions, 3 lessons across eng-manager + /codex passes", "phase": "1", "step": "1.0.5"}
{"ts": "2026-04-23T14:36:55Z", "kind": "decision", "text": "history replay format for first turn after rebind = role-tagged plaintext ([SYSTEM]/[USER]/[ASSISTANT] delimiters flatten canonical turns into single prompt). Adapters may pick different delimiter tokens per vendor tokenizer bias. Lossy vs native multi-turn; only used on first turn after rebind", "subject": "history_replay_format", "why": "non-interactive CLIs take a single text prompt; replay without role delimiters collapses boundaries and lets prior assistant text become prompt-injection fuel. PLAN principle #3 previously hand-waved 'replay full history into the first turn's prompt' without specifying format. Gemini P1 finding from round-3 review.", "source": "PLAN.md § principle 3, gemini P1 #1"}
{"ts": "2026-04-23T14:36:55Z", "kind": "decision", "text": "cancellation DISCARDS backend_session_id (previously 'preserve if backend reported one'). SIGTERMed CLI leaves partial assistant output in its local state; resuming via the poisoned id causes permanent state drift (backend sees User → partial assistant → User retry while canonical shows User → User retry). Next turn starts fresh session with full canonical replay", "subject": "cancellation_discards_session", "why": "decision #5 originally preserved backend_session_id on cancellation for 'next turn can still resume'. Gemini round-3 flagged that this guarantees state drift between backend and canonical history after any cancelled turn. Cost of fresh-session replay is one cold cache tax; cost of state drift is silent corruption of all future turns in that conversation.", "source": "PLAN.md § decision 5, gemini P1 #2"}
{"ts": "2026-04-23T14:36:55Z", "kind": "decision", "text": "CLI-side state isolated per conversation via env-var redirection (CLAUDE_CONFIG_DIR, CODEX_HOME, XDG_CONFIG_HOME+XDG_DATA_HOME for gemini) pointing to <data_dir>/cli-state/<conversation_id>/. OAuth credentials inherited from user global state; only session/cache/history dirs are redirected. On restart, all per-conversation state dirs torn down. Fallback for no-env-var CLIs: per-provider mutex serializing turns on that provider", "subject": "cli_state_isolation", "why": "decision #3 sandboxed cwd but CLIs persist session state in global dirs (~/.claude/, ~/.codex/, ~/.config/gemini/). Concurrent turns across conversations race on gemini -r latest, session index assignment, cross-context leakage. Gemini P1 round-3 finding.", "source": "PLAN.md § decision 16, gemini P1 #3"}
{"ts": "2026-04-23T14:36:55Z", "kind": "decision", "text": "codex --ephemeral is NOT used; codex uses 'exec resume <thread_id>' for turn continuation, matching claude's -r pattern. --ephemeral contradicts the resume path: no on-disk state means no thread to resume on turn 2. CLI state cleanup handled via per-conversation state dirs (decision #16), not --ephemeral", "subject": "codex_uses_resume_not_ephemeral", "why": "ROADMAP phase 3 originally said 'Codex --ephemeral mode is used' alongside 'resume via session id on subsequent turns' — mutually exclusive. Per-conversation state dirs + torn-down on restart gives the same no-persistence-across-restarts property without breaking resume. Gemini P1 round-3 finding.", "source": "ROADMAP.md § phase 3, gemini P1 #4"}
{"ts": "2026-04-23T14:37:14Z", "kind": "decision", "text": "UsageDelta schema always keyed by sub-model: UsageDelta.models = dict[str, ModelUsage] where ModelUsage = {input_tokens, output_tokens, cached_input_tokens}. Single-model providers (claude, codex) report one entry; compound providers (gemini) report multiple entries per turn matching stats.models breakdown. Top-level totals are derived (sum)", "subject": "usage_delta_schema", "why": "UsageDelta was previously under-specified as 'per-turn token usage' — shape didn't require the models dict. Gemini auto-routes across sub-models within a single turn (spike: gemini-3-flash-preview + gemini-2.5-flash-lite in one call); flat {input, output} discards the per-sub-model breakdown and makes quota tracking per sub-model impossible. Gemini P2 round-3 finding, refines the earlier gemini_compound lesson (JOURNAL line 8).", "source": "PLAN.md § principle 1 (Delta union), gemini P2 #5"}
{"ts": "2026-04-23T14:37:15Z", "kind": "lesson", "text": "router derived view over <data_dir>/events.jsonl must be maintained IN-MEMORY and updated incrementally on each event append — NOT re-scanned from file on every routing decision. File is durability; memory is operational. Scan cost grows unbounded with uptime; at 10k turns/day, a week of events is 70k lines / ~10MB — fine for personal use now, pathological at scale. Phase 4 implementation concern", "severity": "medium", "subject": "events_jsonl_derived_view", "source": "gemini P2 #6, defer to phase 4"}
{"ts": "2026-04-23T14:37:15Z", "kind": "lesson", "text": "codex emits 'item.completed' events, not incremental text deltas. CodexAdapter will yield exactly one TextDelta at turn end (not token-by-token). /v1/chat/completions with stream=true against codex-backed conversation will look like a hang-then-burst to clients expecting token streaming. Two options: (a) document honestly (README + server startup log), (b) investigate whether codex has an undocumented sub-item text streaming event. Phase 2 concern for stream-parity across adapters", "severity": "low", "subject": "codex_streaming_illusion", "source": "gemini P3 #7, defer to phase 2"}
{"ts": "2026-04-23T14:38:00Z", "kind": "step_done", "text": "architecture review round 3 (gemini) complete — 7 findings, 3 PLAN revisions + 1 new decision + 1 ROADMAP revision, 5 decisions, 2 lessons. Step 1.0.5 cumulative totals across all three rounds: 30 findings, 22 decisions, 5 lessons", "phase": "1", "step": "1.0.5", "round": "3"}
